<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Product</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/main.css">
</head>

<body>
  <div class="container">
    <h1>
      <%= heading %>
    </h1>

    <% if (locals.product) { %>
      <form id="editForm" method="post" action="/api/products/<%=product.id%>">
        <input type="text" name="title" value="<%=product.title %>">
        <input type="number" name="price" step="0.01" min="0.00" value="<%=product.price %>">
        
        <textarea name="description" rows="10"><%=product.description %></textarea>
        <input type="number" name="quantity" step="0.001" min="0.000" value="<%=product.title %>" required>
      
        <select class="form-select" id="unit" name="unit" required>
            <option value="Pkt">Packets</option>
            <option value="Kg">Kilograms</option>
        </select><br>
        <input type="text" name="seller" value="<%=product.seller %>">
        <button class="full-width" type="submit">
          <%= submit %>
        </button>
      </form>
      <% } else {%>
        <form id="newproductForm" method="post" action="/api/products">
          <input type="text" name="title" placeholder="Title" required>
          <input type="number" id="price" name="price" step="0.01" min="0.00" placeholder="price in ₹" required>
          <textarea name="description" placeholder="description" rows="10"></textarea>
          <input type="number" name="quantity" step="0.001" min="0.000" placeholder="quantity" required>
          <select class="form-select" name="unit" required>
            <option value="" disabled selected hidden>Select Unit</option>
            <option value="packets">Packets</option>
            <option value="kg">Kilograms</option>
        </select><br><br>
          <input  type="text" name="seller" placeholder="Sold by" required>
          <button class="full-width" type="submit">
            <%= submit %>
          </button>
        </form>
        <% } %>

  </div>
</body>

</html>
